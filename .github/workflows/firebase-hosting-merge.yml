# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create .env file
        run: |
          echo "VITE_FIREBASE_API_KEY=AIzaSyCOEaVYl3r_mGhE3BzwSzoH4cBz4kZ_zZQ" > .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=dansharia.firebaseapp.com" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=dansharia" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=dansharia.firebasestorage.app" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=547887124610" >> .env
          echo "VITE_FIREBASE_APP_ID=1:547887124610:web:4e579412d87471d24be49b" >> .env
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DANSHARIA }}
          channelId: live
          projectId: dansharia
